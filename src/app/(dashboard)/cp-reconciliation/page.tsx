import { PagePlaceholder } from "~/components/dashboard/page-placeholder";

export default function ReconciliationPage() {
  return (
    <PagePlaceholder
      title="Reconciliation"
      description="Match incoming payments against expected commissions."
    />
  );
}
