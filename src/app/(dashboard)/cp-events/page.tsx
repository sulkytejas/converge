import { PagePlaceholder } from "~/components/dashboard/page-placeholder";

export default function EventsPage() {
  return (
    <PagePlaceholder
      title="Events"
      description="Schedule, publish, and manage upcoming events."
    />
  );
}
